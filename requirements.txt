# Discord bot framework
discord.py>=2.3.2

# Environment variable loading
python-dotenv>=1.0.0

# Async PostgreSQL driver
asyncpg>=0.29.0

# Image generation for profile cards
Pillow>=10.0.0

# Async HTTP client (avatar download + OpenRouter AI calls)
aiohttp>=3.9.0

# OpenCV for match scoreboard OCR (headless — no GUI, works on Linux servers)
opencv-python-headless>=4.8.0

# Numerical processing (required by OpenCV)
numpy>=1.24.0,<2.0.0

# Tesseract OCR Python wrapper (requires tesseract binary — see railway.toml / nixpacks.toml)
pytesseract>=0.3.10
