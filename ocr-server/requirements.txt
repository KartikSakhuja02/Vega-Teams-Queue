# RunPod OCR Server — Python Dependencies
# All pinned to minimum-compatible versions; use latest compatible at build time.

# ── RunPod Serverless ─────────────────────────────────────────────────────────
runpod>=1.7.3

# ── Vision-Language Model ─────────────────────────────────────────────────────
# transformers >= 4.46 required for Qwen2.5-VL support
transformers>=4.46.0
accelerate>=0.34.0
# HuggingFace Hub for model download
huggingface_hub>=0.25.0
# Qwen2.5-VL specific utilities
qwen-vl-utils>=0.0.8
# einops required by Qwen2.5-VL
einops>=0.8.0

# ── Image processing ─────────────────────────────────────────────────────────
# Headless (no GUI) variant — required for server environments
opencv-python-headless>=4.9.0
Pillow>=10.4.0
numpy>=1.26.0,<2.0.0

# ── Utilities ────────────────────────────────────────────────────────────────
# SafeTensors for faster model loading
safetensors>=0.4.0
